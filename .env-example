ENV="dev"
BOT_TOKEN="<your-telegram-bot-token>"
ADMINS_ID="<comma-separated-admin-ids>"
DB_USERNAME="<db-user>"
DB_PASSWORD="<db-password>"
DB_DATABASE="<db-name>"
DB_PORT="<db-port>"
DATABASE_URL_ASYNC="postgresql+asyncpg://${DB_USERNAME}:${DB_PASSWORD}@db:${DB_PORT}/${DB_NAME}"
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@db:${DB_PORT}/${DB_NAME}"
